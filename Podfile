# Uncomment the next line to define a global platform for your project
 platform :ios, '10.0'

target 'SampleCIAndCD' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
	pod 'SDWebImage'

  # Pods for SampleCIAndCD

  target 'SampleCIAndCDTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SampleCIAndCDUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
